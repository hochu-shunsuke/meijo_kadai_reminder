# 名城大学生向け 課題管理リマインドシステム

## 概要
このシステムは、名城大学のWebClassおよびGoogle Classroomで出された課題を自動的に取得し、Google Tasks（ToDoリスト）に一括登録するツールです。

Googleスプレッドシート上で動作し、一度設定を行えば、毎日決まった時間に自動で課題を確認・更新します。スマートフォンにGoogle Tasksアプリを入れておけば、課題の締め切り通知をスマホで受け取ることができるようになります。

## 特徴
*   **設定はメニューから**: スプレッドシートのメニューから数回クリックするだけで設定が完了します。
*   **自動更新**: 指定した時間にシステムが自動で起動し、新しい課題がないかチェックします（1日に複数回の設定も可能）。
*   **一元管理**: WebClassとGoogle Classroomの両方の課題を、Google Tasksという一つのアプリでまとめて管理できます。
*   **スマホ通知**: Google Tasksアプリをインストールすることで、課題の締め切り前に通知を受け取ることができます。

## 使い方（セットアップ手順）

スプレッドシートを開き、以下の手順で設定を行ってください。

### 事前準備
*   PCでの操作を推奨します。
*   複数のGoogleアカウントにログインしている場合、うまく動作しないことがあります。その場合は、Google Chromeの「シークレットウィンドウ」などを使い、大学のGoogleアカウントのみでログインした状態でスプレッドシートを開いてください。

### 手順1: WebClassの認証設定
1.  スプレッドシート上部のメニューバーにある **「✨ 課題自動取得システム」** をクリックします。
    *   ※ 初回実行時は「承認」を求められる画面が表示されます。画面の指示に従って承認（許可）を行ってください。
2.  表示されたメニューから **「1. WebClass 認証情報を設定 (ID/PW)」** を選択します。
3.  設定画面が表示されるので、WebClassのログインに使用する **学籍番号** と **パスワード** を入力し、「保存」をクリックします。
    *   入力されたパスワードは、あなた自身のGoogleアカウント内の安全な領域に保存され、外部に送信されることはありません。

### 手順2: 通知と自動実行の設定
1.  再度メニューの **「✨ 課題自動取得システム」** をクリックします。
2.  **「2. Tasks・自動実行設定を完了 (初回のみ)」** を選択します。
3.  以下の項目を設定し、「設定を保存して連携」をクリックします。
    *   **Tasksリスト名**: Google Tasksに作成されるリストの名前（例: 大学課題）。
    *   **実行時間**: 何時ごろに課題チェックを行うか。カンマ区切りで複数指定できます（例: `6,18` で1日2回）。
    *   **削除猶予**: 完了した課題をリストから削除するまでの日数。

### 手順3: 動作確認
1.  メニューから **「3. 今すぐ実行（テスト）」** を選択します。
2.  スクリプトが実行され、WebClassやClassroomから課題が取得されます。
3.  Google Tasks（Gmailの右サイドバーやスマホアプリ）を開き、指定したリストに課題が追加されていることを確認してください。

以上で設定は完了です。以降は毎日自動的に課題が更新されます。

## 技術的な解説

このシステムは Google Apps Script (GAS) を使用して構築されています。本リポジトリのコードに関心がある方向けに、技術的な仕組みを解説します。

### システム構成
*   **プラットフォーム**: Google Apps Script (Container-bound Script)
*   **フロントエンド**: HTML Service (Bootstrap 5) を使用した設定ダイアログ
*   **データ保存**: ScriptProperties（認証情報など）、Spreadsheet（ログ、取得データ一時保存）

### 処理フロー
1.  **認証**:
    *   `WebClassClient.js` が WebClass のログイン処理を担当します。
    *   `UrlFetchApp` を使用し、SSO認証およびSAML認証のリダイレクトフローを追跡してセッションCookieを取得・維持します。
2.  **データ取得**:
    *   **WebClass**: ログイン後のダッシュボードHTMLを解析 (`Parser.js`) し、受講中のコース一覧と各コース内の課題情報をスクレイピングします。
    *   **Google Classroom**: Advanced Google Services の Classroom API を使用して、正規の方法で課題データを取得します。
3.  **データ整形と登録**:
    *   取得したデータは `AppLogic.js` で整理され、重複チェックを行った上で Google Tasks API を通じてユーザーのタスクリストに追加されます。
4.  **自動実行**:
    *   GASのTime-drivenトリガーを使用し、ユーザーが指定した時間に `Main.js` のエントリーポイント関数を実行します。

### ファイル構成
*   `Main.js`: アプリケーションのエントリーポイント。メニュー作成、トリガー設定、各ダイアログの呼び出しを行います。
*   `AppLogic.js`: 課題取得からTasks登録までのメインロジックを記述しています。
*   `WebClassClient.js`: WebClassへのHTTPリクエスト、Cookie管理、リダイレクト処理をカプセル化したクラスです。
*   `Parser.js`: WebClassのHTMLから必要な情報を抽出するための正規表現ロジックを含みます。
*   `Config.js`: URLや正規表現などの定数定義ファイルです。
*   `Utils.js`: ログ出力、設定値の保存・読み出し、共通ヘルパー関数などをまとめています。
*   `Setting.html` / `Setting_Tasks.html`: 設定ダイアログのUI定義ファイルです。
*   `Check.js`: 課題の取りこぼしをチェックする確認用機能です。自動実行からは呼ばれません。
*   `appsscript.json`: マニフェスト。OAuthスコープを**明示的に固定**しています（後述）。

いずれも `gas/` ディレクトリに置いてあります。Apps Scriptエディタへは手動でコピーして反映します（clasp等は使っていません）。

## 注意事項
*   WebClassの仕様変更（HTML構造の変更など）により、正しく動作しなくなる可能性があります。
*   パスワードなどの機密情報は `PropertiesService.getUserProperties()` を使用してユーザーごとのプロパティストアに保存されます。これはスクリプトの実行者（ユーザー自身）のみがアクセス可能な領域です。
*   本システムは大学公式のものではありません。

## トラブルシューティング

### `PERMISSION_DENIED`「ストレージからの読み取り中にサーバーエラーが発生しました」

**ブラウザにログインしているアカウントと、スクリプトの所有者が食い違っているのが原因です。** コードやスコープの問題ではありません。

GCPコンソールなど別の用事で個人のGoogleアカウントにログインすると、ブラウザのデフォルトアカウント（`/u/0`）がそちらにズレて発生します。

*   確認: Apps ScriptのURLが `script.google.com/u/0/...` になっているか。`/u/1/` などなら別アカウントで開いています。
*   対処: Chromeのシークレットウィンドウで、**大学のGoogleアカウントだけ**でログインして開き直してください。

### Classroomの課題だけ取得できない

ログに `The caller does not have permission` が出る場合、OAuthスコープが不足しています。

`courses.courseWork.list` には **`classroom.coursework.me.readonly`（学生向け）** が必要ですが、
Apps Scriptのスコープ自動推論では教師向けの `classroom.coursework.students.readonly` が選ばれることがあり、
その場合「自分が教えていないコース」に対して権限エラーになります。

`appsscript.json` の `oauthScopes` で必要なスコープを明示することで解決します。
変更後は https://myaccount.google.com/permissions で**既存の認可を取り消してから**再実行してください。
取り消さないと古いスコープのまま動き、同じエラーが出続けます。

### OAuthスコープを変更したら別の権限エラーが出た

`appsscript.json` に `oauthScopes` を書くと**スコープの自動推論が無効になります**。
新しいGoogleサービスを使うコードを足した場合は、対応するスコープを手動で追加してください。

## 設計上の判断メモ

### GitHub Actionsへの移行を検討したが中止した（2026-09-15）

「スプレッドシート上で動いているのが気持ち悪い」という動機で、GitHub Actions + Node への全面移行を検討しました。
技術的には可能で、WebClassのSAMLログインがNodeで動くことまで検証しましたが、**中止しました**（検証用コードはリポジトリに残していません）。

理由は、気持ち悪さの正体が構成ではなく **「課題が取得できていないのに、黙って動き続けていたこと」** だったためです。
必須要件は「課題がちゃんと取れること」と「壊れたときに気づけること」であり、
どちらもGASのまま解決できました（Classroomのスコープ修正 ＋ 不具合タスクの通知）。

移行した場合に発生するコスト（個人GCPプロジェクトのOAuth設定、大学ドメインから見て第三者アプリ扱いになる問題、
Secretsの管理、cronの遅延）に対して、得られるものが釣り合わないと判断しています。

## 未対応の既知の問題

### 締切が延長された課題が、二度とTasksに登録されない

1. 締切が過ぎると、その課題の行に `EXPIRED` が付く（ここまでは正しい）
2. 先生が締切を延長すると、シートの締切欄は新しい値に更新されるが、
   `EXPIRED` フラグはリンクを基準に引き継がれる
3. 登録判定が `TERMINAL_FLAGS`（`EXPIRED` を含む）で弾くため、登録されない
4. `_cleanup` は締切が未来の行を消さないため、行も消えず状態が固定される

結果として、締切が有効なのにToDoに入らず、エラーも出ない状態になる。

同じ経路で `SKIPPED_NODATE`（期限が無い資料に後から締切が付いた場合）も
起こりうるが、こちらは `_cleanup` が毎回行を削除するため次回の実行で
フラグごと作り直され、自動的に回復する。**詰むのは `EXPIRED` のみ。**

暫定的な発見方法として、メニューの「課題の取りこぼしをチェック」で
`★要確認` として検出できる。

修正案: 登録判定から `EXPIRED` を外し、「登録しない恒久的な理由」ではなく
「現時点では期限切れ」という再評価される状態として扱う。
`COMPLETED` / `DELETED` は利用者の意思なので従来どおり維持する。

## 変更履歴

### 2026-09-15 (6)

*   **メニューに「課題の取りこぼしをチェック」を追加しました。**
    WebClassとClassroomから見えている全項目と、実際のGoogle Tasksの中身を突き合わせ、
    ToDoに入っていない項目とその理由を一覧にします。
    「期限切れ」「期限が設定されていない」など入らなくて正しいものと、
    「期限が有効なのにToDoに無い」「期限を解釈できない」という本当の取りこぼしを区別します。
    シートにもTasksにも一切書き込まない、読み取りだけの確認用機能です。
*   **Classroomの期限を計算する処理を `fromClassroomDue` に集約しました。**

### 2026-09-15 (5) 無言で失敗する経路を塞ぐ

「課題が取得できていないのに、エラーも出ない」状態を作らないための変更。

*   **課題タイトルに新着バッジの「New」が混入する不具合を、実際に修正しました。**
    2026-09-15 (1) で修正したつもりでしたが、検証用のコードにしか反映されておらず、
    `Parser.js` には入っていませんでした。
    除去の正規表現が `<div class="cl-contentsList_new">` への完全一致を要求していたため、
    クラスが増える・タグが `span` になる・属性が付くだけで外れていました。
    タグ名と属性に依存しない形に変更しています。
*   **Tasksリストが削除・改名されても自動で復旧するようにしました。**
    従来は保存済みのIDを消して終了するだけで、メニューから手動で再設定するまで
    課題が一件もTasksに登録されない状態が続きました。しかも不具合タスクの置き場所も
    失われるため、何の通知も出せませんでした。
    設定に残っているリスト名から探し直し、無ければ作成します。
    通信エラーとリスト削除を区別し、通信エラーでは設定を消しません。
*   **期限が読み取れずTasksに登録されなかった課題を、ログに残すようにしました。**
    期限欄が空（資料などの対象外項目）と、値があるのに解釈できない（日付表記の変更が疑われる）
    を区別し、後者だけ警告します。
*   **コースは取得できたのに全コースで0件だった場合に警告するようにしました。**
    スクレイピングが壊れても、コース一覧さえ取れれば無言で0件になっていました。

### 2026-09-15 (4)

*   **WebClassのログをClassroomと同じ粒度に揃えました。**
    従来はWebClass側にコース別のログが無く、失敗したときしか記録されませんでした。
    コース数と、コースごとの「項目N件 / 期限付きM件」を出力します。
    WebClassの「項目」には課題だけでなく資料なども含まれるため、
    実際にTasksの対象になる「期限付き」の件数を併記しています。
*   **ログの保持行数を2000行から5000行に引き上げました。**
    上のログ追加で1実行あたり約34行になったため、
    5000行で約150実行分（1日2回なら約2か月半）を保持します。
    ログシートは2列しか使わないので約1万セルで、
    スプレッドシート全体の上限（1000万セル）から見れば誤差の範囲です。

### 2026-09-15 (3) リファクタリング

*   **実行の開始・終了のログに日時と所要時間を含めるようにしました。**
    従来は日時がタイムスタンプ列にしかなく、メッセージだけをコピーすると失われていました。
*   **ログシートが無限に伸びないよう、直近2000行だけ残すようにしました。**
*   **User-Agentを1セッションにつき1つに固定しました。**
    従来はリクエストごとにランダムに選び直しており、1回のログインの途中で
    端末が変わったように見える状態でした。UAをセッションに紐づける実装では
    認証が通らなくなる恐れがあります。
*   **`processTasksSync` から、実質的に何もしていなかった約30行を削除しました。**
    同じシートを2回読み込んで2つのマップを作り、`row[6]` に `row[6]` を代入していました。
    Tasks ID / フラグの復元は `SheetUtils.writeToSheet` が取得時に済ませています。
*   **列番号とフラグ文字列を `Config.js` の定数（`COL` / `FLAG` / `TERMINAL_FLAGS`）に集約しました。**
    従来は `row[6]` のような添字と `'COMPLETED'` のような文字列が各所に直接書かれており、
    タイポしてもエラーにならず静かに条件が外れる状態でした。
*   **`_cleanup` の判定を整理し、`REGISTERED` のまま残り続ける行も掃除するようにしました。**
    期限から猶予期間が過ぎた行は状態によらず削除します（期限が未来の行は従来どおり保持）。

### 2026-09-15 (2)

*   **一時的な取得失敗が原因で、同じ課題がTasksに二重登録される不具合を修正。**
    シート更新時に「今回取得できた分」だけで全体を上書きしていたため、
    Google API側の一時的な障害（`The service is currently unavailable`）で
    1コース取得に失敗すると、その課題の行がTasks IDごと消え、
    次回の成功時に未登録とみなされて再登録されていました。
    取得できなかった行でも、Tasksへ登録済み・処理済みのものは保持するようにしました。
*   **Google APIの一時的な障害に対して、指数バックオフで最大2回再試行するようにしました。**
    権限エラーなど恒久的な失敗は再試行しません。
    WebClass（大学のサーバ）へのリクエストは、無駄な負荷をかけないため再試行の対象外です。

### 2026-09-15

*   **Classroomの課題が取得できない不具合を修正。** `appsscript.json` を追加し、`classroom.coursework.me.readonly` を含むOAuthスコープを明示的に固定しました。
*   **Classroomの締切が9時間早くズレていた不具合を修正。** Classroom APIの `dueDate`/`dueTime` は仕様上UTCですが、これをローカル時刻として組み立てていました。
*   **1コースの取得失敗でClassroom全体が取得できなくなる問題を修正。** コース単位で例外を捕捉するようにしました。
*   **取得結果が0件のときに、既存のシートを空で上書きしないようにしました。**
*   **コース・課題一覧のページネーションに対応しました。**（`nextPageToken` を処理していなかったため、件数が多いと取りこぼしていました）
*   **異常があった場合、Google Tasksに「不具合タスク」を1件だけ登録するようにしました。** ログシートを開かなくても、いつも見ているウィジェットで異常に気づけます。常に1件だけで毎日増えず、復旧すると自動的に消えます。
*   **1日に複数回の自動実行に対応しました。** 設定画面の実行時間にカンマ区切りで入力します（例: `6,18`）。設定のたびに古いトリガーを全削除して作り直すため、重複しません。
*   **WebClassの課題タイトルに新着バッジの「New」が混入する不具合を修正しました。**
